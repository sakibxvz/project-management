# Ignore node_modules in both client and server
client/node_modules/
server/node_modules/

# Ignore .env files in both client and server
client/.env
server/.env

# Ignore log files in both client and server
client/npm-debug.log*
server/npm-debug.log*

# Ignore lock files (optional if you don't want to track them)
client/package-lock.json
server/package-lock.json

# Ignore system-specific or temporary files
.DS_Store
Thumbs.db

# Ignore coverage folder
client/coverage/
server/coverage/

# Ignore build folders (if they exist in your project)
client/.next/
server/dist/
